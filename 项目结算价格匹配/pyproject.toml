[project]
name = "settlement-price-matcher"
version = "0.1.0"
description = "项目结算价格精确匹配与查询工具"
requires-python = ">=3.10,<3.15"
dependencies = [
    "flask>=3.1.0",
    "openpyxl>=3.1.5",
    "xlrd>=2.0.1",
]

[project.scripts]
settlement-price-matcher = "app:main"

[tool.uv]
package = false

